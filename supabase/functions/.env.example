# Local QA Edge Function secrets only.
# This file is safe to commit because these values protect synthetic Local data only.
# Never reuse these values in Production.

PIN_PEPPER=forestring-local-qa-pin-pepper-v1
RATE_LIMIT_PEPPER=forestring-local-qa-rate-pepper-v1
